package rawdb

import (
	"testing"
)

func TestNewS3DB(t *testing.T) {
	// accKey := ""
	// secretKey := ""
	// region := "ap-east-1"
	// bktPrefix := "dev-arseed"
	// endpoint := ""
	// s3db, err := NewS3DB(accKey, secretKey, region, bktPrefix, endpoint)
	// assert.NoError(t, err)
	// bkt := schema.BundleItemBinary
	// key := "00EF2rptAE2TaiikThn8OuWS3DD8KLw7Q_1GL5oP_Jk"
	// t.Log(time.Now().String())
	// exist := s3db.Exist(bkt, key)
	// t.Log(exist)
	// t.Log(time.Now().String())
}
